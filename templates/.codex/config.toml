# Diffmogger optional MCP configuration.
# Servers are disabled in static config so they do not start unnecessarily.
# Diffmogger wrappers mount enabled project MCPs as temporary per-role `codex exec -c` overrides from resolved intake/dashboard/manifest state; profiles are present for manual use.
# Diffmogger does not run `codex mcp add`, `codex mcp login`, or mutate user/global Codex config.

{{MCP_CODEX_CONFIG}}
